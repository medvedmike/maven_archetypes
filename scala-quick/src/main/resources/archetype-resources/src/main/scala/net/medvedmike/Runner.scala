package net.medvedmike

import org.apache.log4j.Logger

object Runner extends App {
  private val log = Logger.getLogger(classOf[Runner])
  log info "Starting ..."
  //...
  log info "Shutdown ..."
}

class Runner {

}
